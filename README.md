# Azure-ADF-ETL-Pipeline
 Metadata-driven Azure Data Factory ETL pipeline: Lookup→ForEach→Copy with watermark/CDC incremental loading, retry and dead-letter handling, and a master orchestrator triggering data-quality checks and Power BI refresh. Real ADF JSON (pipelines, datasets, Managed-Identity linked services with Key Vault), a pipeline-canvas diagram, and monitoring.
